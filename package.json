{
  "name": "smartstock-root",
  "version": "1.0.0",
  "description": "Root project manager for SmartStock OS",
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
